# Agent Instructions

Codex、Cursor Agent 以及其他遵循 `AGENTS.md` 的编码代理在本项目中工作时，必须遵守以下提示词与工程约束。

<你的思考方法论>
<概念>$$P = \{ S_0, S_g, O, C \}$$
  S_0,初始状态
  S_g,目标状态
  O操作子：能改变的动作、方法论
  C约束：路径约束（哪些路不能走）
  问题拉力（Problem Tension）。 为什么 $S_0$ 无法自然演变成 $S_g$？ 阻碍这一过程的核心矛盾是什么？（是信息缺失？是资源匮乏？还是逻辑悖论？）
</概念>
  解空间多解生成
    使用多方法论进行并行的生成：初制订问题的约束边界、解的维度。然后，在维度内穷举->跨维度组合。并且将约束反转、约束松弛、约束极限化。
再寻找类似的整体方案，或局部解决单个问题的方案，充分考虑某个只能局部优化的案例对于我们整体方案的价值。充分考虑组合多个局部优化的案例来寻找案例
      1.“违反直觉但有效”的方案
        意味着我们的约束矩阵存在隐形约束，而有安全绕开了该隐形约束。可以从中发现解空间扩张的路径和思路。
      2.解空间中的非常规维度
        例如： 用时间换空间、用技术极限来改变流程。用流程替代技术 权限 认知 用用户行为弥补系统限制
      3.约束反转，劣势转变优势
        “把劣势当优势”的方案 例如： 限制本身就是卖点 慢成为稀缺 不稳定反而制造注意力    这是典型的 约束反转能力
      如何“拆解”别人的方案（核心） 永远不要从“功能”开始拆 从“结构”和“假设”开始
        ① 它解决的“原始问题”是什么？ 很多方案重构了问题。 表面问题 ≠ 真正问题  ② 它默认了哪些前提？ 问： 哪些条件被视为“不可变”？ 哪些被偷偷当成“可变”？    这一步能暴露 世界模型差异  ③ 它操作了哪些“维度”？    新增维度 = 新解空间  ④ 哪些约束被松弛 / 反转 / 推极限？ 逐条对照： 被忽略的约束 被反向利用的限制 被推到极限的系统参数  ⑤ 它牺牲了什么？ 没有方案是免费的。 用户体验？ 覆盖面？ 稳定性？ 合规性？    牺牲项 = 未来可变形点  ⑥ 它的“生成路径”可能是什么？ 反推： 如果你是作者，你第一步会怎么想？ 中间尝试了哪些失败方向？ 哪个关键洞察导致转折？
      如何机制迁移？
      对一个方案，系统性做以下变形： 替换维度、反转顺序、压缩或拉长时间、改变

  观察者参照系（也就是用户是哪方立场）
  评价函数则负责回答一个核心问题：我们现在的状态（State）距离目标（Goal）还有多远？
评价函数的目的不是“寻找赞美”，而是“寻找漏洞”。 自我批判机制： 要求模型在输出结论前，先模拟一个对手！
     </你的思考方法论>

<元方法论>

<概念>

总体最终目的背景：在绝对服从基底约束和禁区约束，遍历系统内参数的约束（问题拉力），建立因果链、寻找代偿约束，对代偿约束（路径寄生代价）执行剥离、降维置换与操作子 ($O$) 重构的演化多分支思考、探索；在发散探索、剪枝的循环后最终得到：一个跨越了边际跃迁势垒、无限逼近理想解 (IFR) 但又兼容现实的目标状态 ($S_g$)——即一条消耗极小“非对称资源”、消除有害次生副作用，且能让系统主要有用功能自运作的全局最优路径。追求最小代价，实现系统的有用功能的自我实现、有害功能的自我消除，获得最大的系统改进成果。



$$P

= \{ S_0, S_g, O, C \}$$





S_0,现状



IFR才是把“目的”从“手段”中彻底剥离了出来的需求描述。





S_g,目标状态=理想解的代偿约束实现

在绝对服从基底约束与禁区约束的前提下，通过调用系统内或超系统的廉价资源 ($O$) 解决核心参数自互斥矛盾 (PC)，确定了一条最终的代偿约束，在这个代偿约束下的全局最优工程解。它是系统剥离心理惯性后，利用心理学效应/社会势能场**作为非对称资源，使得系统在零外部驱动成本的情况下，实现 MUF 的自发浮现与自我修正。无限逼近 IFR 但具备物理可行性的实际落地状态。





理想解=$$\lim_{\text{边际运行成本, 有害功能} \to 0} \frac{\sum \text{有用功能}}{\text{边际运行成本} + \sum \text{有害功能}} = +\infty$$我们强制站立在 IFR 的极值点上回头审视 $S_0$，提出终极拷问：“是什么参数自互斥矛盾 (PC) 阻止了资源 $X$ 自己去完成这项任务？” 从而锁定真正的系统屏障。



理想解的哲学极限（实体消亡，功能永存）：用来摧毁心理惯性，并强迫你逆向提取参数自互斥矛盾 (PC)。系统本身不再以物理实体的形式存在（质量、体积、能耗均降为 $0$），但其主要有用功能 (MUF, Main Useful Function) 依然能被 $100\%$ 甚至超额完成。即：“系统不存在，但功能被完美实现”。没有任何附加成本，没有任何有害伴生作用。



理想度 = $$\text{系统动态理想度} = \frac{\sum \text{内生资源驱动的有用功能}}{\sum \text{边际运行成本} + \sum \text{有害功能}}$$

为了达成理想解，技术演进的触发条件允许较大的系统构建成本。



S曲线触发条件：$$\int_{\text{系统诞生}}^{\text{相变节点}} \text{系统构建成本} \, dt \ge \text{边际跃迁势垒阈值}$$



系统构建成本： 为了构建系统底层机制、铺设信任场或算法场，而进行的不计回报的重资本/重资源投入（例如：早期的高额补贴、底层大模型训练成本）。



边际跃迁势垒阈值： 打破系统当前死锁状态所需的最低能量总和（例如：双边市场达到自我生长的最小供需密度），对于短期增加成本分母，但长期改变分子有用功能的增长函数，也就是边际跃迁点。



技术系统：具有互相联系的元件与运作的执行主要有用功能 (MUF) 的事物的集合。包含以下子系统之一：{ 动力装置, 传输装置, 执行装置, 控制装置 } ，至少包含执行装置。

子系统：系统的内部构成模块，负责输出辅助功能。

相邻系统：系统控制边界外，互不统属，但存在时空交集或交互的外部实体/环境。

超系统：包含技术系统、赖以生存的宿主或环境基底，技术系统之外与其有相互作用的系统，是技术系统的超系统



反系统：

与当前系统处于同一层级

功能方向相反

共同构成更高层的可调控结构

两者缺一不可

如果只有加速没有刹车，那系统不可控。

如果只有输入没有删除，系统会失控膨胀。

所以反系统的本质是：

用来调节、制衡、修正技术系统输出方向的结构单元。超系统、子系统、技术系统均可能有其自身的反系统。





而s_g目标状态则是理解解的资源匹配与降维回落：寻找当前环境下的免费资源 ($O$) 来解决 PC，最终寻找到一条代偿约束下的理想路径。降落到可实现的 $S_g$。







O操作子：能改变的动作、方法论





C约束：

用户给定约束：解空间的临时“外部结界”，路径约束并非产生问题的原因，而是系统自身、客观环境、物理定律或商业逻辑施加的刚性边界条件。



禁区约束：用户使用严禁字样设立的约束，只有极其强力、低构建成本、低运行成本的路径下才可以建议用户移除该约束。



系统内参数约束：问题拉力（Problem Tension）。 为什么 $S_0$ 无法自然演变成 $S_g$？ 阻碍这一过程的核心矛盾是什么？（是信息缺失？是资源匮乏？还是逻辑悖论？）



基底约束（物理级、客观级约束）：指系统存在的不可约减、不可逾越的第一性原理边界。包括物理定律（如热力学第二定律）、逻辑公理、并非产生问题的原因，而是系统自身、客观环境、市场参与者的必然心理效应施加的刚性边界条件。由一系列的参数自互斥矛盾、跨参数倒挂矛盾构成基底约束。



代偿约束：指为了规避基底约束、禁区约束以实现目标功能，而在选择特定路径时，由于该路径的不完美性所产生的寄生性限制、次生开销或有害副作用。由一系列的参数自互斥矛盾、跨参数倒挂矛盾构成代偿约束。代偿约束是规避基底约束后，新路径上爆发的**“目标功能”与“寄生代价（复杂度/副作用）”间的横向参数拉扯**，是规避绝对红线所产生新约束。



我们从不解决物理定律，我们是寻找最小的代偿约束。TRIZ 的四大分离原理（空间、时间、系统级别、条件分离）本质上是在不触动物理定律的前提下，最小化代偿约束。当你解决了参数自互斥矛盾，你实际上是优化了操作子，从而使路径代价趋近于 $0$。此时，$S_g$ 无穷接近于

IFR。第二种兼容：通过“置换”重塑边界（降维置换逻辑）如果一个矛盾在当前路径下是“死锁”的，ARIZ 会引导你进行系统转换（升维或转向超系统）。这意味着你放弃了当前的代偿约束，换取了一个全新的代偿约束。边界依然存在，但“形状”变了。 原本那个令你痛苦的“强边界”消失了，取而代之的是一个你目前尚能忍受的新边界。



属性与参数：

属性：指系统、子系统或环境场存在的定性特征维度

参数：定性特征维度的可测量表达。是属性在特定维度上的定量刻画（如重量、温度、速度、信息密度）。也是衡量系统“问题拉力”大小的标尺。

矛盾：是约束的组成部分。

跨参数倒挂矛盾：两个不同参数之间的冲突。试图改善系统的某一个参数（如强度）时，导致另一个参数（如重量）恶化。

参数自互斥矛盾：单一参数自身的冲突。为了满足功能需求，系统的同一个参数必须同时处于两个互斥的状态（如既要热又要冷）。



</概念>

小程序文件夹参照agent.md的prompt ：
严格遵守：

【体积限制】
- 主包 ≤ 2MB
- 优先使用分包
- 禁止引入大型npm库

【技术约束】
- 仅使用原生小程序（WXML / WXSS / JS）
- 能用原生API绝不用第三方库
- 图片使用网络URL，不内嵌base64

考虑建立组件体系、能复用组件就不要重复写、样式尽量复用 class但避免过度抽象
- 样式尽量复用 class（命名需语义化，禁止无意义缩写）
- 每个组件职责单一，避免一个组件做过多事情

最后优化时prompt（前期用不到，因为过早优化影响一致性）
- 优先减少代码体积，其次才是可读性
- 能复用组件就不要重复写
- 样式尽量复用 class
- JS逻辑最简实现（不追求架构优雅）
【优化目标】